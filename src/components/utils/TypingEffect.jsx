// This function is used for implementing typing effect
const TypingEffect = ({ text }) => {
    // const typingEffect = () =>
    //     {
    //         let text = textList[ textListIndex % textListLength ];

    //         if (isTypingForward) {
    //             setTimeout(() =>
    //             {
    //                 textNode.textContent += text[ textIndex ];
    //                 textIndex++;

    //                 if (textIndex === text.length) {
    //                     isTypingForward = false;
    //                     setTimeout(typingEffect, 500);
    //                 } else {
    //                     typingEffect();
    //                 }
    //             }, 200);
    //         } else {
    //             setTimeout(() =>
    //             {
    //                 textNode.textContent = textNode.textContent.slice(0, textIndex - 1);
    //                 textIndex--;

    //                 if (textIndex === 0) {
    //                     isTypingForward = true;
    //                     textListIndex++;
    //                     setTimeout(typingEffect, 500);
    //                 } else {
    //                     typingEffect();
    //                 }
    //             }, 200);
    //         }
    //     }

    return <div></div>;
};

export default TypingEffect;
